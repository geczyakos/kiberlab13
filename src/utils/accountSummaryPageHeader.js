// Builds the response headers for the "my account" summary page,
// which shows the customer's upcoming bookings and payment history.

export function buildAccountPageHeaders() {
  return {
    'Content-Type': 'text/html',
  };
}